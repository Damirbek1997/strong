spring:
  profiles:
    active: ${PROFILE}
  main:
    allow-bean-definition-overriding: true

management:
  endpoint:
    info:
      enabled: true
  endpoints:
    web:
      exposure:
        include: health,metrics,prometheus,loggers

logging:
  level:
    root: info
